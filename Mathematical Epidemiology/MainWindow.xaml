<Window x:Class="Mathematical_Epidemiology.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:charting="clr-namespace:System.Windows.Controls.DataVisualization.Charting;assembly=System.Windows.Controls.DataVisualization.Toolkit"
        Title="Mathematical Epidimiology" Height="350" Width="525">

    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>
                <MenuItem Header="_Close"/>
                <MenuItem Header="_Save"/>
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom" Height="20">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                <TextBlock Name="lblTime" Text="3 sec"></TextBlock>
            </StatusBarItem>
            <Separator Grid.Column ="1"></Separator>
            <StatusBarItem Grid.Column ="2" HorizontalContentAlignment="Stretch">
                <ProgressBar Name="progressBar" Value="50" Height="10"></ProgressBar>
            </StatusBarItem>
        </StatusBar>
        <TabControl>
            <TabItem Header="Simple">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="200"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <charting:Chart></charting:Chart>
                    <GridSplitter Grid.Column="1"  Width="5"
                        VerticalAlignment="Stretch" HorizontalAlignment="Center">
                    </GridSplitter>
                    <Grid>

                    </Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Advanced"></TabItem>
        </TabControl>
    </DockPanel>
</Window>
